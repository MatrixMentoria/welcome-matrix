git clone = baixa o projeto para a máquina.

git add = adiciona os arquivos ao commits.

git commint -m “mensagem” = formalizar mudanças adicionando uma mensagem
para explicar.

git push = para subir as alterações no GitHub.

git pull = para receber o código atualizado.

git help = para acessar a ajuda.
